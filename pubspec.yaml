name: admin_panel
description: Admin Panel Web App
publish_to: 'none'
environment:
  sdk: ">=2.19.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  cloud_firestore: ^5.6.0
  provider: ^6.0.5
  flutter_dotenv: ^6.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true

